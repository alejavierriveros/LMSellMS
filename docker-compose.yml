version: '3.8'

services:
  lmsellms:
    build: .
    container_name: lmsellms
    ports:
      - "8082:8080" 
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - SPRING_APPLICATION_NAME=LMSELLMS
      - SERVER_PORT=8080  
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://host.docker.internal:8090/eureka
      - EUREKA_INSTANCE_PREFER_IP_ADDRESS=false 
      - EUREKA_INSTANCE_HOSTNAME=host.docker.internal 
      - SPRING_DATASOURCE_URL=jdbc:mysql://host.docker.internal:3306/ventas_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=
    networks:
      - libmark-network

networks:
  libmark-network:
    external: true